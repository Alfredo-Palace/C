#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

int calcHorasLaboradas(int pin);
int Registro(void);
int verAsistencias(int pin);
int verTiempoTrabajado(int pin);
